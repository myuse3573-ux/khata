# Logs
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Server secrets — NEVER commit these
server/.env

# SQLite database (server manages this)
server/*.db
server/*.db-shm
server/*.db-wal

# Migration artifacts (keep backups, not the live DB)
server/*.migrated
server/migration.log

# Old flat JSON database (migrated)
server/data.db.json
